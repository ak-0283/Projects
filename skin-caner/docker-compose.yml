version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    command: sh -c "gunicorn --workers 1 --threads 4 --timeout 120 --bind 0.0.0.0:5000 app:app"
    restart: unless-stopped
    container_name: skincare-ai-app
